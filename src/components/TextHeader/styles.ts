import styled from 'styled-components';

const StyledTextHeader = styled.div`
  color: black;
  font-size: 24px;
  font-weight: bold;
`;

const Container = styled.div`
  margin-top: 30px;
  margin-left: 24px;
  margin-right: 24px;
  display: flex;
  justify-content: space-between;
`;

export { StyledTextHeader, Container };
