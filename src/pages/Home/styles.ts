import styled from 'styled-components';

const FullScreenMediaControlCard = styled.div`
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  z-index: 8;
`;

export { FullScreenMediaControlCard };
